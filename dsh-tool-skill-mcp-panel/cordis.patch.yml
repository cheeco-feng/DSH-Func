# dsh-tool-skill-mcp-panel bundle patch.
# When this package is installed with `dsh plugin --profile <p> add ...`,
# the profile auto-appends it to dsh.profile.bundles and applies this layer,
# so the plugin mounts without any manual cordis.patch.yml editing.
- insert:
    - id: tool-skill-mcp-panel
      name: '@cheeco/dsh-tool-skill-mcp-panel'
